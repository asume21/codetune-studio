services:
  - type: web
    name: codetune-studio
    runtime: node
    buildCommand: bash build.sh
    startCommand: node start-production.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        fromService:
          type: web
          name: codetune-studio
          property: port
      - key: XAI_API_KEY
        sync: false
      - key: DATABASE_URL
        sync: false
    autoDeploy: true